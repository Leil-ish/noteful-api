CREATE TABLE notes (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  note_name TEXT NOT NULL,
  folder_id INTEGER REFERENCES folders(id) ON DELETE RESTRICT NOT NULL,
  modified TIMESTAMP DEFAULT now(),
  content TEXT
);